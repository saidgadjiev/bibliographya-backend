spring:
    datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
      userName: sa
      password: sa
    profiles:
      active: test
app:
  jwt:
    secret: test
